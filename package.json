{
  "name": "clubhub",
  "description": "A central website for all clubs and organizations on campus.",
  "authors": "Coding Hub",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "deploy": "firebase deploy",
    "backend:deploy": "cd backend && yarn run deploy",
    "backend": "cd packages/backend && yarn run express",
    "frontend": "cd packages/frontend && yarn run start"
  }
}
