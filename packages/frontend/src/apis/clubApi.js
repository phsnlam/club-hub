import { clubEndpoint } from './endpoints'
const axios = require('axios')

export const getAllClubs = async () => {
  // Example for your reference (will need changes from you to make it works)
  // try {
  //   const result = await axios.get(clubEndpoint).then(response => response)
  //   let clubs = [] // put clubs data got from result to clubs
  //   return { clubs }
  // } catch (error) {
  //   console.log('Error when getAllClub')
  //   return {
  //     error: {
  //       message: 'Unknown error'
  //     }
  //   }
  // }
}
